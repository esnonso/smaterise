/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "d3uuw27oeu4o94.cloudfront.net",
      },
    ],
  },
};

export default nextConfig;
